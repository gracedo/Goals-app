<h1><%= @goal.name %></h1>

<p><%= link_to "Edit Goal", edit_user_goal_url(@goal.user, @goal) %></p>

Completed? <%= button_to "\u2713", user_goal_completed_url(@goal.user, @goal) %>

<h3>Comments</h3>

<% @goal_comments.each do |comment| %>
	<p><%= comment.body %><br>
	Posted by <%= comment.poster.username %> at <%= comment.created_at %><br>
	<%= button_to "Delete Comment", user_goal_goal_comment_url(@goal.user, @goal, comment), method: :delete %></p>
<% end %>

<h4>Add Comment</h4>
<form action="<%= user_goal_goal_comments_url(@goal.user, @goal) %>" method="POST" accept-charset="utf-8">

	<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

	<input type="text" name="goal_comment[body]" id="comment">

	<p><input type="submit" value="Add Comment"></p>
</form>


<p><%= link_to "Back to all goals", user_goals_url(@goal.user) %></p>